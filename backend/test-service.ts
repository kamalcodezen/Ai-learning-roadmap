import prisma from './src/lib/prisma.js'; import { getOrGenerateLearningPath } from './src/modules/learner/roadmap/services/learning-path.service.js'; async function main() { const user = await prisma.user.findFirst(); if (!user) return console.log('No users found'); console.log('Testing for user:', user.id); try { const data = await getOrGenerateLearningPath(user.id); console.log('Success:', JSON.stringify(data, null, 2)); } catch (error) { console.error('Error:', error); } } main();
